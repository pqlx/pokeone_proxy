syntax = "proto2";
package PSXAPI.Response1317789099;
import "bcl.proto"; // schema for protobuf-net's handling of core .NET types

message Reorder {
   repeated .bcl.Guid Pokemon = 1;
   optional int32 Box = 2 [default = 0];
}

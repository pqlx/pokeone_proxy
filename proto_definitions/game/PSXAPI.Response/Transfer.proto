syntax = "proto2";
package PSXAPI.Response83;
import "bcl.proto"; // schema for protobuf-net's handling of core .NET types

message Transfer {
   optional TransferResult Result = 1 [default = Failed];
   optional .bcl.Guid Pokemon = 2 ;
   optional int32 Box = 3 [default = 0];
}
enum TransferResult {
   Failed = 0;
   Success = 1;
}

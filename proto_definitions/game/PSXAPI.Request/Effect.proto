syntax = "proto2";
package PSXAPI.Request17;
import "bcl.proto"; // schema for protobuf-net's handling of core .NET types

message Effect {
   optional EffectAction Action = 1 [default = Cancel];
   optional .bcl.Guid UID = 2 ;
}
enum EffectAction {
   Cancel = 0;
   Use = 1;
}

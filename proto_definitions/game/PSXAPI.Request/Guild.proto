syntax = "proto2";
package PSXAPI.Request24;

message Guild {
   optional GuildAction Action = 1 [default = Request];
   optional string Data = 2;
   optional bytes EmblemData = 3;
}
enum GuildAction {
   Request = 0;
   Create = 1;
   Leave = 2;
   Message = 5;
   Emblem = 6;
}

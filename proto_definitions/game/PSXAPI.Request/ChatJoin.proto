syntax = "proto2";
package PSXAPI.Request11;

message ChatJoin {
   required string Channel = 1;
   optional ChatJoinAction Action = 2 [default = Join];
}
enum ChatJoinAction {
   Join = 0;
   Leave = 1;
}
